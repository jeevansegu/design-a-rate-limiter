services:
    redis:
        image: redis:latest
        container_name: my-rate-limiter-redis
        ports:
            - '6379:6379'
        volumes:
            - redis-data:/data
        command: redis-server --requirepass ${REDIS_PASS} --appendonly yes
        restart: unless-stopped

volumes:
    redis-data:
        driver: local
